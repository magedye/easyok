# =============================================================================
# EasyData — Local Environment Example
# =============================================================================
# PURPOSE
# -------
# This file documents ALL local-only and test-only environment flags.
# It MUST NOT be used in production.
#
# Copy to: .env.local
# Modify ONLY for local development and testing.
#
# GOVERNANCE
# ----------
# Any usage of the flags below outside ENV=local is a violation.
# =============================================================================


# =============================================================================
# Environment
# =============================================================================
ENV=local


# =============================================================================
# ⚠️ DANGEROUS FLAGS — TEST / LOCAL ONLY
# =============================================================================
# WARNING:
# --------
# These flags disable or bypass critical security and governance controls.
# They are permitted ONLY for:
# - Local development
# - Test environments
# - CI sandboxing
#
# They MUST NEVER be enabled in production.
# =============================================================================


# -----------------------------------------------------------------------------
# Authentication & Authorization Bypass
# -----------------------------------------------------------------------------
# Disables all authentication and authorization layers.
AUTH_ENABLED=false
RBAC_ENABLED=false
RLS_ENABLED=false


# -----------------------------------------------------------------------------
# Admin Access Bypass (ADR-0014)
# -----------------------------------------------------------------------------
# Allows access to /admin/* endpoints without a real admin role.
# Effective ONLY when ENV=local.
ADMIN_LOCAL_BYPASS=true


# -----------------------------------------------------------------------------
# Training Readiness & Governance Guards
# -----------------------------------------------------------------------------
# Disables training readiness enforcement.
# Allows the application to start even if blocked SQL attempts exist.
ENABLE_TRAINING_PILOT=false
TRAINING_READINESS_ENFORCED=false


# -----------------------------------------------------------------------------
# Integration & Legacy Test Gates
# -----------------------------------------------------------------------------
# Enables execution of legacy and integration pytest suites.
RUN_INTEGRATION_TESTS=true
RUN_ORACLE_TESTS=true
RUN_TELEMETRY_TESTS=true


# -----------------------------------------------------------------------------
# Operational Constraints
# -----------------------------------------------------------------------------
# Disables runtime safety constraints for testing and debugging.
ENABLE_RATE_LIMIT=false
ENABLE_AUDIT_LOGGING=false


# -----------------------------------------------------------------------------
# Telemetry / Observability
# -----------------------------------------------------------------------------
# Disables telemetry exporters to avoid local noise or incompatibilities.
ENABLE_TELEMETRY=false
ENABLE_OTEL=false


# -----------------------------------------------------------------------------
# SQLGuard Overrides
# -----------------------------------------------------------------------------
# NEVER enable in production.
# Allows destructive SQL operations during controlled tests only.
SQLGUARD_ALLOW_DDL=false
SQLGUARD_ALLOW_DML=false


# =============================================================================
# END OF LOCAL-ONLY DANGEROUS FLAGS
# =============================================================================
