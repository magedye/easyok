name: 🛡️ EasyData Fortress CI
on: [push, pull_request]

jobs:
  fortress-pipeline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 🧹 Free Disk Space (Maximize Runner Space)
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf "/usr/local/share/boost"
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"
      # --- الجزء الخاص ببايثون (تسريع + فحص تعارضات) ---
      - name: 🐍 Setup Python & Cache
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 🔍 Audit & Install Backend
        run: |
          pip install pip-audit
          pip install -r requirements.txt
          pip-audit # فحص تعارضات الحزم أمنياً

      - name: 🏛️ Architectural Breach Check
        run: |
          pip install -e ./flake8_easydata_arch/
          flake8 app # الفحص الذي يمنع تداخل الطبقات (EDA901-903)

      # --- الجزء الخاص بالفرونت إيند (تسريع + فحص تعارضات) ---
      - name: 📦 Setup Node & Cache
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: 🧪 Frontend Audit & Test
        run: |
          cd frontend
          npm ci
          npm audit
          npm test

      # --- الجزء الخاص بالعقد (OpenAPI Validation) ---
      - name: 📜 Validate Master Contract
        run: npx @stoplight/spectral-cli lint easydata-fortress.yaml