# 📄 وثيقة متطلبات البرمجيات (Software Requirements Specification - SRS)

هذه الوثيقة تحدد المتطلبات الوظيفية وغير الوظيفية لنظام EasyData، والتي يجب أن يُحققها المنتج النهائي. تستند المتطلبات إلى وثائق النظام الأصلية والنقاشات السابقة.

## 1. تعريف المصطلحات

- **السؤال (Question):** استعلام بلغة طبيعية يطرحه المستخدم للحصول على معلومات من قاعدة البيانات.
- **RAG (Retrieval‑Augmented Generation):** آلية تعتمد على البحث في المخزن المتجهات واستدعاء نموذج لغوي لتوليد استعلام SQL.
- **المستخدم (User):** شخص يحمل هوية رقمية وصلاحيات محددة لاستعمال النظام.
- **الواجهة الأمامية (Frontend):** تطبيق ويب يتواصل مع الواجهة الخلفية عبر REST/SSE.

## 2. المتطلبات الوظيفية (Functional Requirements)

1. **FR1: إدارة الأسئلة**
   - يجب أن يكون المستخدم قادراً على إرسال سؤال باللغة العربية أو الإنجليزية للحصول على نتيجة.
   - يجب عرض الافتراضات التي استخلصها النموذج وإعطاء المستخدم إمكانية تعديلها أو تثبيتها.

2. **FR2: توليد SQL وتنفيذه**
   - يجب على النظام توليد استعلام SQL صالح يتوافق مع قاعدة البيانات المتصلة (Oracle أو MSSQL)، ويطبق القيود الأمنية تلقائياً.
   - يجب تنفيذ الاستعلام بصلاحيات قراءة فقط.
   - في حال فشل الاستعلام الأول، يجب محاولة تصحيحه وإعادة التنفيذ بحد أقصى 3 محاولات.

3. **FR3: بث النتائج**
   - يجب بث النتيجة على ثلاث مراحل: بيانات، تكوين الرسم البياني، ملخص نصي.
   - يجب أن يتم البث عبر Server‑Sent Events (SSE) لإتاحة تحديث النتائج في الوقت الفعلي.

4. **FR4: إدارة التدريب**
   - يجب توفير واجهة للمسؤولين لرفع أزواج سؤال‑SQL، أو نصوص توثيقية، أو ملفات DDL للتدريب.
   - يجب أن يتمكن المسؤول من الموافقة أو الرفض لهذه العناصر.

5. **FR5: الملاحظات وتقييم الجودة**
   - يجب أن يتمكن المستخدم من تقييم نتيجة السؤال (صحيح/خاطئ) مع تعليق اختياري.
   - يجب تسجيل التقييمات في قاعدة بيانات النظام لتطوير أداء النموذج.

6. **FR6: إدارة المستخدمين**
   - يجب توفير آلية لإضافة مستخدمين جدد وتعيين أدوارهم (Admin, Manager, User, Guest).
   - يجب أن تكون هناك صلاحيّات مختلفة بناءً على الدور المعيّن.

7. **FR7: تسجيل التدقيق (Audit Logging)**
   - يجب تسجيل كل سؤال، والاستعلام الناتج، ونتيجة التنفيذ في جدول `audit_logs` مع معلومات المستخدم.

8. **FR8: الصحة والمراقبة**
   - يجب توفير نقطة نهاية (/health) تعرض حالة التطبيق.
   - يجب أن يتم تسجيل مؤشرات الأداء (زمن الرد، معدّل الأخطاء) في سجلات بنظام قابل للمراقبة.

## 3. المتطلبات غير الوظيفية (Non‑Functional Requirements)

1. **NFR1: الأداء**
   - يجب أن يكون متوسط زمن الاستجابة أقل من 5 ثوانٍ للاستعلام العادي.
   - يجب دعم بث نتائج أكبر من 100 صف في أقل من 10 ثوانٍ.

2. **NFR2: الأمان**
   - يجب تشفير كل الاتصالات بين الواجهة الأمامية والخلفية عبر HTTPS.
   - يجب تطبيق سياسة RBAC لحماية نقاط النهاية الحساسة.
   - يجب أن تكون بيانات الاعتماد مخزنة في ملفات البيئة وغير مضمَّنة في الكود.

3. **NFR3: الموثوقية**
   - يجب ألا تتسبب فشل مزود LLM أو قاعدة البيانات في انهيار النظام؛ يتم استخدام Circuit Breaker لإظهار خطأ مناسب للمستخدم.
   - يجب أن تحتفظ الخدمة بسجلات الأخطاء لمراجعتها لاحقاً.

4. **NFR4: القابلية للصيانة**
   - يجب كتابة الكود وفقاً لمبادئ النظافة (PEP8)، مع إرفاق اختبارات وحدية.
   - يجب أن تكون الإعدادات قابلة للتبديل دون تعديل الكود (عبر `.env`).

5. **NFR5: التوافق**
   - يجب دعم تشغيل التطبيق على بيئات Windows وLinux.
   - يجب أن يكون التطبيق قابلاً للتشغيل في حاوية Docker.

6. **NFR6: التدويل**
   - يجب أن يدعم النظام استلام أسئلة باللغة العربية والإنجليزية وإرجاع ملخصات بالعربية عند وجود السؤال بالعربية.

7. **NFR7: التكامل**
   - يجب إمكانية إضافة قنوات تواصل إضافية (Teams، WhatsApp، Slack) في المستقبل عبر API موحد.

## 4. قيود (Constraints)

1. النظام يعتمد على توفر الإنترنت للوصول إلى مزودات LLM الخارجية (OpenAI/Google) عند استخدام تلك الخدمة.
2. يجب أن يكون لدى المستخدم الصلاحيات المناسبة في قاعدة البيانات (قراءة فقط) حتى يعمل الاستعلام بنجاح.
3. الحد الأعلى للصفوف المعادة افتراضياً هو 100 صف؛ يمكن تغييره في الإعدادات إذا لزم الأمر.

## 5. السيناريوهات الأساسية للاستخدام (Use Cases)

1. **استخدام عادي:** مستخدم يرسل سؤالًا، يستقبل البيانات، يرى الرسم البياني والملخص، يقيّم النتيجة، وربما يثبتها في لوحة القيادة.
2. **إضافة تدريب:** مسؤول يرفع استعلامًا معتمدًا وينتظر الموافقة.
3. **مراجعة التدقيق:** مدير يستعرض سجل الاستعلامات والأخطاء للتحقق من الاستخدام.

## 6. المصادقة والتطوير

يجب مراجعة هذه الوثيقة بانتظام مع أي تغيير في نطاق المشروع أو إضافة متطلبات جديدة. تُعتبر هذه الوثيقة ملزمة لكل من يعمل على تطوير النظام.