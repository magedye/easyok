import React from 'react';
import ReactDOM from 'react-dom/client';
import { BrowserRouter } from 'react-router-dom';
import App from './App';
import './index.css';
import { bootstrapRuntimeEnv } from './utils/runtimeEnv';

// Entry point for the React application.
// We wrap the app with BrowserRouter to enable routing.
bootstrapRuntimeEnv();

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>
);
