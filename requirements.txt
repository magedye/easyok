# ============================================================================
# Core Framework (Updated for TestClient compatibility)
# ============================================================================
fastapi>=0.115.6
uvicorn[standard]>=0.32.0
pydantic>=2.10.3
pydantic-settings>=2.6.1
numpy>=1.26.0,<2.0.0

# ============================================================================
# Database / ORM (Updated to silence SQLAlchemy 2.0 warnings)
# ============================================================================
sqlalchemy>=2.0.36
oracledb==2.5.0
pyodbc==5.2.0
asyncpg>=0.30.0
sqlparse==0.5.3

# ============================================================================
# Vector Stores
# ============================================================================
chromadb>=0.5.23
qdrant-client>=1.12.1

# ============================================================================
# AI Core & LLM Clients
# ============================================================================
vanna==2.0.1
openai>=1.55.0

# ============================================================================
# Security (Modern standards)
# ============================================================================
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# ============================================================================
# SQL / Parsing (CRITICAL PINS - Maintained as per project governance)
# ============================================================================
sqlglot>=25.0.0,<29.0.0

# ============================================================================
# Observability / Telemetry
# ============================================================================
opentelemetry-api>=1.28.0
opentelemetry-sdk>=1.28.0
opentelemetry-exporter-otlp>=1.28.0
opentelemetry-instrumentation-fastapi==0.49b1
prometheus-client>=0.21.0
structlog>=24.4.0

# ============================================================================
# Utilities & HTTP (Fixed for Test Stability)
# ============================================================================
python-dotenv==1.0.1
ujson==5.10.0
redis>=5.2.0
httpx==0.28.1
regex==2024.11.6
typing-inspect==0.9.0

# ============================================================================
# Testing Tools (Mandatory for EasyData Fortress)
# ============================================================================
pytest>=8.3.4
pytest-asyncio>=0.25.0
# هذا الأمر سيحدث كل شيء في الملف ولكن سيتجاهل أي حزمة مثبتة مسبقاً بنفس الإصدار
#pip install -r requirements.txt --no-cache-dir